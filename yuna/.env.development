# Local development environment (Vite reads this automatically)
# Points the frontend to the local server running on port 4000
VITE_SOCKET_URL=http://localhost:4000
VITE_API_URL=http://localhost:4000/api
